name: Renovate
on:
  workflow_dispatch:
  push:
    paths:
      - '**/Cargo.toml'
      - '**/Cargo.lock'
      - '**/renovate.json'
    branches:
      - main

jobs:
  call_renovate:
    runs-on: ubuntu-latest

    steps:
      - name: Hit Renovate Webhook
        run: curl -X POST https://n8n.jonahmakowski.ca/webhook/renovate
